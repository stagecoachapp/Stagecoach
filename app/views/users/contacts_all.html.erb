<div class="contacts-top">
	<%= render 'contacts_page_controls' %>
</div>
<h4>Unsorted</h4>
<div class="contacts-list">
	<ul>
		<% for contact in @contacts %>
		<%= link_to contact.name, contact %>
		<% end %>
	</ul>
</div>